<resources>

    <!---->
    <!-- Base -->
    <!---->

    <style name="AppThemeBase" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@android:color/black</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- statusBar -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- windowBackground -->
        <item name="android:windowBackground">@color/windowBackground</item>
        <!-- AppBar -->
        <item name="actionBarTheme">@style/AppTheme.AppBarOverlay</item>
        <!-- ActionMode -->
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/CustomActionMode</item>
        <!-- AlertDialog -->
        <item name="alertDialogTheme">@style/AlertDialogTheme</item>
        <!-- BottomSheetDialog -->
        <item name="bottomSheetDialogTheme">@style/CustomBottomSheetDialog</item>
        <!-- Switch -->
        <item name="colorSwitchThumbNormal">@color/subTextFitBackground</item>
    </style>

    <style name="AppTheme" parent="AppThemeBase" />

    <style name="AppTheme.Translucent">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Base - Activity Theme for PhotoView -->
    <style name="AppTheme.Translucent.PhotoView">
        <item name="android:statusBarColor">@android:color/black</item>
    </style>

    <!---->
    <!-- AppBar -->
    <!---->

    <style name="AppTheme.Toolbar">
        <item name="android:layout_height">?actionBarSize</item>
        <item name="android:background">?colorPrimary</item>
        <item name="popupTheme">@style/AppTheme.PopupOverlay</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="titleTextColor">@android:color/white</item>
        <item name="subtitleTextColor">@android:color/white</item>
        <!-- button -->
        <item name="android:colorControlNormal">@android:color/white</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.DayNight" />

    <!-- for PhotoView -->
    <style name="AppTheme.AppBarOverlay.PhotoView" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="elevation">0dp</item>
        <item name="android:translationZ">.1dp</item>
    </style>

    <!---->
    <!-- action mode -->
    <!---->

    <style name="CustomActionMode" parent="Widget.AppCompat.ActionMode">
        <item name="background">?colorPrimary</item>
    </style>

    <!---->
    <!-- AlertDialog -->
    <!---->

    <style name="AlertDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
        <item name="buttonBarNeutralButtonStyle">@style/NeutralButtonStyle</item>
    </style>

    <style name="PositiveButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">?colorAccent</item>
        <item name="rippleColor">@color/colorGray144</item>
    </style>

    <style name="NegativeButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/textFitBackground</item>
        <item name="rippleColor">@color/colorGray144</item>
    </style>

    <style name="NeutralButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/textFitBackground</item>
        <item name="rippleColor">@color/colorGray144</item>
    </style>

    <!---->
    <!-- MaterialButton -->
    <!---->

    <style name="CommonMaterialButton" parent="Widget.MaterialComponents.Button">
        <item name="android:gravity">center</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Medium</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:elevation">4dp</item>
        <item name="cornerRadius">8dp</item>
        <item name="backgroundTint">@color/colorAccent</item>
    </style>

    <style name="MaterialButtonNoElevation" parent="Widget.MaterialComponents.Button">
        <item name="android:stateListAnimator">@null</item>
        <item name="android:elevation">0dp</item>
    </style>

    <style name="TransparentMaterialButton" parent="Widget.MaterialComponents.Button">
        <item name="materialThemeOverlay">@style/TransparentButtonThemeOverlay</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:elevation">0dp</item>
    </style>

    <style name="TransparentButtonThemeOverlay">
        <item name="colorPrimary">@color/colorTransparent</item>
    </style>

    <!---->
    <!-- TextView -->
    <!---->

    <style name="OneLineTextViewEllipsizeStart" parent="Widget.AppCompat.TextView">
        <item name="android:singleLine">true</item>
        <item name="android:ellipsize">start</item>
    </style>

    <style name="OneLineTextView" parent="Widget.AppCompat.TextView">
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="TwoLineTextView" parent="Widget.AppCompat.TextView">
        <item name="android:maxLines">2</item>
        <item name="android:ellipsize">end</item>
    </style>

    <!---->
    <!-- BottomSheetDialog -->
    <!---->

    <style name="CustomBottomSheetDialog" parent="@style/ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheet</item>
    </style>

    <style name="CustomBottomSheet" parent="Widget.MaterialComponents.BottomSheet">
        <item name="shapeAppearanceOverlay">@style/CustomShapeAppearanceBottomSheetDialog</item>
    </style>

    <style name="CustomShapeAppearanceBottomSheetDialog">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopRight">16dp</item>
        <item name="cornerSizeTopLeft">16dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>